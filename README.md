# React Basics – Ticket System Project

This project demonstrates core React concepts including JSX, State, Props, hooks, state sharing, and event handling.

---

## 1. What is JSX, and why is it used?

**JSX (JavaScript XML)** is a syntax extension for JavaScript that allows you to write HTML-like code directly in JavaScript.

Example:

```jsx
const element = <h1>Hello, World!</h1>;
